# Virtual environment
venv/

# Generated output files (regenerated by running the scripts - not source)
*.xlsx
# Exception: one intentionally-committed sample output, for anyone who wants
# to explore a real workbook's live formulas/charts without running the tool -
# clearly labeled as a static snapshot, not live data (see README).
!examples/*.xlsx

# Python cache
__pycache__/
*.pyc

# pytest cache (generated every test run, never meant to be committed)
.pytest_cache/

# API keys - NEVER commit these
.env

# OS junk
.DS_Store
Thumbs.db
